apiVersion: v1
kind: Service
metadata:
  name: {{ include "vclusterfront.fullname" . }}
  labels:
    {{- include "vclusterfront.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vclusterfront.selectorLabels" . | nindent 4 }}

